version: '3.8'

services:
  app:
    build: .
    container_name: lexai-legal-assistant
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - PORT=8000
      - HOST=0.0.0.0
    volumes:
      - ./uploads:/app/uploads
    restart: unless-stopped
